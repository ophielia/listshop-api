apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "listshop-api.fullname" . }}-test-connection"
  labels:
    { { - include "listshop-api.labels" . | nindent 4 } }
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "listshop-api.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
